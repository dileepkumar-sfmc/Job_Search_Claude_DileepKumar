# Dependencies
node_modules/

# Build output
dist/

# Env / secrets — never commit real API keys (but .env.example IS committed)
.env
.env.local
.env.*.local
server/.env
server/.env.local

# Local Claude settings (machine-specific)
.claude/settings.local.json

# Hook bookkeeping (records the last change-set the CLAUDE.md sync hook reminded about)
.claude/.claudemd-review-hash

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
